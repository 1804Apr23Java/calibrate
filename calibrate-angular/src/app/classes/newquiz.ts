import { Library } from './library';

export class NewQuiz {
    quizName: String;
    libraries: Library[];
    quizSize: number;
    // fromHardestQuestions: boolean;  //Collin said wait on this one
}